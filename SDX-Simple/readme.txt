This is the SDX controller for SDX slice. It provides rest API for customer slices on ExoGENI and Chameleon to stitch to it.

To build:
./scritps/build.sh

To create a sdx slice:
./scripts/createslice.sh -c config/sdx.conf

To run:
./scritps/sdxserver.sh -c config/sdx.conf 
